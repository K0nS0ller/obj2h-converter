# OBJ2H

# How to compile?

You can compile this with the command:
```bash
make
```
then install/uninstall:
```bash
sudo make install
sudo make uninstall
```

# Usage

```bash
obj2h /path/to/input.obj /path/to/out.h
```
